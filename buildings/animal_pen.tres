[gd_resource type="Resource" script_class="BuildingDef" load_steps=3 format=3]

[ext_resource type="Script" path="res://scripts/basebuilder/building_def.gd" id="1"]
[ext_resource type="PackedScene" path="res://scenes/buildings/animal_pen.tscn" id="2"]

[resource]
script = ExtResource("1")
building_name = "Animal Pen"
category = "pen"
footprint_size = Vector2i(2, 2)
resource_cost = {"wood": 20, "food": 15}
build_time_seconds = 6.0
scene = ExtResource("2")
max_health = 60
passive_resource_output = {"food": 6}
description = "Passive food income at dawn (bonus on generated animal-zone tiles). The chickens raise the alarm the moment a raid gets close, and a Dog patrols the pen, fighting nearby enemies for free - doesn't use up villager population."
